# 🔌 API Notes — Live Cricket Score App

This document describes the external cricket data API used in the project
and how it is configured.

---

## API Provider
- CricAPI / RapidAPI (Cricket Data API)

---

## Environment Configuration

The API key is stored securely using environment variables.

Example `.env` file:

```env
VITE_CRICAPI_KEY=your_api_key_here
